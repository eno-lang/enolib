# Expecting sections but getting a field

-- input
field: value
-- input

-- javascript
parse(input).sections('field');
-- javascript

-- php
Enolib\Parser::parse($input)->sections('field');
-- php

-- python
enolib.parse(input).sections('field')
-- python

-- ruby
Enolib.parse(input).sections('field')
-- ruby

## ValidationError

-- text
Only sections with the key 'field' were expected.
-- text

-- snippet
   Line | Content
 >    1 | field: value
-- snippet

selection: [0,0] => [0,12]


# Expecting sections but getting a field with continuations

-- input
field:
| continuation
| continuation
-- input

-- javascript
parse(input).sections('field');
-- javascript

-- php
Enolib\Parser::parse($input)->sections('field');
-- php

-- python
enolib.parse(input).sections('field')
-- python

-- ruby
Enolib.parse(input).sections('field')
-- ruby

## ValidationError

-- text
Only sections with the key 'field' were expected.
-- text

-- snippet
   Line | Content
 >    1 | field:
 *    2 | | continuation
 *    3 | | continuation
-- snippet

selection: [0,0] => [2,14]

# Expecting sections but getting a field with continuations separated by idle lines

-- input
field: value
| continuation
| continuation

> comment
| continuation
-- input

-- javascript
parse(input).sections('field');
-- javascript

-- php
Enolib\Parser::parse($input)->sections('field');
-- php

-- python
enolib.parse(input).sections('field')
-- python

-- ruby
Enolib.parse(input).sections('field')
-- ruby

## ValidationError

-- text
Only sections with the key 'field' were expected.
-- text

-- snippet
   Line | Content
 >    1 | field: value
 *    2 | | continuation
 *    3 | | continuation
 *    4 | 
 *    5 | > comment
 *    6 | | continuation
-- snippet

selection: [0,0] => [5,14]

# Expecting sections but getting a field with one attribute

-- input
element:
attribute = value
-- input

-- javascript
parse(input).sections('element');
-- javascript

-- php
Enolib\Parser::parse($input)->sections('element');
-- php

-- python
enolib.parse(input).sections('element')
-- python

-- ruby
Enolib.parse(input).sections('element')
-- ruby

## ValidationError

-- text
Only sections with the key 'element' were expected.
-- text

-- snippet
   Line | Content
 >    1 | element:
 *    2 | attribute = value
-- snippet

selection: [0,0] => [1,17]


# Expecting sections but getting a field with empty lines and three attributes

-- input
element:

attribute = value

attribute = value

attribute = value

-- input

-- javascript
parse(input).sections('element');
-- javascript

-- php
Enolib\Parser::parse($input)->sections('element');
-- php

-- python
enolib.parse(input).sections('element')
-- python

-- ruby
Enolib.parse(input).sections('element')
-- ruby

## ValidationError

-- text
Only sections with the key 'element' were expected.
-- text

-- snippet
   Line | Content
 >    1 | element:
 *    2 | 
 *    3 | attribute = value
 *    4 | 
 *    5 | attribute = value
 *    6 | 
 *    7 | attribute = value
      8 | 
-- snippet

selection: [0,0] => [6,17]

# Expecting sections but getting a field with two attribute with comments

-- input
element:
> comment
attribute = value

> comment
attribute = value
-- input

-- javascript
parse(input).sections('element');
-- javascript

-- php
Enolib\Parser::parse($input)->sections('element');
-- php

-- python
enolib.parse(input).sections('element')
-- python

-- ruby
Enolib.parse(input).sections('element')
-- ruby

## ValidationError

-- text
Only sections with the key 'element' were expected.
-- text

-- snippet
   Line | Content
 >    1 | element:
 *    2 | > comment
 *    3 | attribute = value
 *    4 | 
 *    5 | > comment
 *    6 | attribute = value
-- snippet

selection: [0,0] => [5,17]

# Expecting sections but getting a field with one item

-- input
element:
- item
-- input

-- javascript
parse(input).sections('element');
-- javascript

-- php
Enolib\Parser::parse($input)->sections('element');
-- php

-- python
enolib.parse(input).sections('element')
-- python

-- ruby
Enolib.parse(input).sections('element')
-- ruby

## ValidationError

-- text
Only sections with the key 'element' were expected.
-- text

-- snippet
   Line | Content
 >    1 | element:
 *    2 | - item
-- snippet

selection: [0,0] => [1,6]


# Expecting sections but getting a field with empty lines and three items

-- input
element:

- item

- item

- item

-- input

-- javascript
parse(input).sections('element');
-- javascript

-- php
Enolib\Parser::parse($input)->sections('element');
-- php

-- python
enolib.parse(input).sections('element')
-- python

-- ruby
Enolib.parse(input).sections('element')
-- ruby

## ValidationError

-- text
Only sections with the key 'element' were expected.
-- text

-- snippet
   Line | Content
 >    1 | element:
 *    2 | 
 *    3 | - item
 *    4 | 
 *    5 | - item
 *    6 | 
 *    7 | - item
      8 | 
-- snippet

selection: [0,0] => [6,6]

# Expecting sections but getting a field with two items with comments

-- input
element:
> comment
- item

> comment
- item
-- input

-- javascript
parse(input).sections('element');
-- javascript

-- php
Enolib\Parser::parse($input)->sections('element');
-- php

-- python
enolib.parse(input).sections('element')
-- python

-- ruby
Enolib.parse(input).sections('element')
-- ruby

## ValidationError

-- text
Only sections with the key 'element' were expected.
-- text

-- snippet
   Line | Content
 >    1 | element:
 *    2 | > comment
 *    3 | - item
 *    4 | 
 *    5 | > comment
 *    6 | - item
-- snippet

selection: [0,0] => [5,6]

# Expecting sections but getting a flag

-- input
element
-- input

-- javascript
parse(input).sections('element');
-- javascript

-- php
Enolib\Parser::parse($input)->sections('element');
-- php

-- python
enolib.parse(input).sections('element')
-- python

-- ruby
Enolib.parse(input).sections('element')
-- ruby

## ValidationError

-- text
Only sections with the key 'element' were expected.
-- text

-- snippet
   Line | Content
 >    1 | element
-- snippet

selection: [0,0] => [0,7]
