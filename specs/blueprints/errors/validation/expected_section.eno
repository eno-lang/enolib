# Expecting a section but getting a field

-- input
element: value
-- input

-- javascript
parse(input).section('element');
-- javascript

-- php
Enolib\Parser::parse($input)->section('element');
-- php

-- python
enolib.parse(input).section('element')
-- python

-- ruby
Enolib.parse(input).section('element')
-- ruby

## ValidationError

-- text
A section with the key 'element' was expected.
-- text

-- snippet
   Line | Content
 >    1 | element: value
-- snippet

selection: [0,0] => [0,14]

# Expecting a section but getting a field with one attribute

-- input
element:
attribute = value
-- input

-- javascript
parse(input).section('element');
-- javascript

-- php
Enolib\Parser::parse($input)->section('element');
-- php

-- python
enolib.parse(input).section('element')
-- python

-- ruby
Enolib.parse(input).section('element')
-- ruby

## ValidationError

-- text
A section with the key 'element' was expected.
-- text

-- snippet
   Line | Content
 >    1 | element:
 *    2 | attribute = value
-- snippet

selection: [0,0] => [1,17]


# Expecting a section but getting a field with empty lines and three attributes

-- input
element:

attribute = value

attribute = value

attribute = value

-- input

-- javascript
parse(input).section('element');
-- javascript

-- php
Enolib\Parser::parse($input)->section('element');
-- php

-- python
enolib.parse(input).section('element')
-- python

-- ruby
Enolib.parse(input).section('element')
-- ruby

## ValidationError

-- text
A section with the key 'element' was expected.
-- text

-- snippet
   Line | Content
 >    1 | element:
 *    2 | 
 *    3 | attribute = value
 *    4 | 
 *    5 | attribute = value
 *    6 | 
 *    7 | attribute = value
      8 | 
-- snippet

selection: [0,0] => [6,17]

# Expecting a section but getting a field with two attributes with comments

-- input
element:
> comment
attribute = value

> comment
attribute = value
-- input

-- javascript
parse(input).section('element');
-- javascript

-- php
Enolib\Parser::parse($input)->section('element');
-- php

-- python
enolib.parse(input).section('element')
-- python

-- ruby
Enolib.parse(input).section('element')
-- ruby

## ValidationError

-- text
A section with the key 'element' was expected.
-- text

-- snippet
   Line | Content
 >    1 | element:
 *    2 | > comment
 *    3 | attribute = value
 *    4 | 
 *    5 | > comment
 *    6 | attribute = value
-- snippet

selection: [0,0] => [5,17]

# Expecting a section but getting a field with one item

-- input
element:
- item
-- input

-- javascript
parse(input).section('element');
-- javascript

-- php
Enolib\Parser::parse($input)->section('element');
-- php

-- python
enolib.parse(input).section('element')
-- python

-- ruby
Enolib.parse(input).section('element')
-- ruby

## ValidationError

-- text
A section with the key 'element' was expected.
-- text

-- snippet
   Line | Content
 >    1 | element:
 *    2 | - item
-- snippet

selection: [0,0] => [1,6]


# Expecting a section but getting a field with empty lines and three items

-- input
element:

- item

- item

- item

-- input

-- javascript
parse(input).section('element');
-- javascript

-- php
Enolib\Parser::parse($input)->section('element');
-- php

-- python
enolib.parse(input).section('element')
-- python

-- ruby
Enolib.parse(input).section('element')
-- ruby

## ValidationError

-- text
A section with the key 'element' was expected.
-- text

-- snippet
   Line | Content
 >    1 | element:
 *    2 | 
 *    3 | - item
 *    4 | 
 *    5 | - item
 *    6 | 
 *    7 | - item
      8 | 
-- snippet

selection: [0,0] => [6,6]

# Expecting a section but getting a field with two items with comments

-- input
element:
> comment
- item

> comment
- item
-- input

-- javascript
parse(input).section('element');
-- javascript

-- php
Enolib\Parser::parse($input)->section('element');
-- php

-- python
enolib.parse(input).section('element')
-- python

-- ruby
Enolib.parse(input).section('element')
-- ruby

## ValidationError

-- text
A section with the key 'element' was expected.
-- text

-- snippet
   Line | Content
 >    1 | element:
 *    2 | > comment
 *    3 | - item
 *    4 | 
 *    5 | > comment
 *    6 | - item
-- snippet

selection: [0,0] => [5,6]

# Expecting a section but getting a field with continuations

-- input
element:
| continuation
| continuation
-- input

-- javascript
parse(input).section('element');
-- javascript

-- php
Enolib\Parser::parse($input)->section('element');
-- php

-- python
enolib.parse(input).section('element')
-- python

-- ruby
Enolib.parse(input).section('element')
-- ruby

## ValidationError

-- text
A section with the key 'element' was expected.
-- text

-- snippet
   Line | Content
 >    1 | element:
 *    2 | | continuation
 *    3 | | continuation
-- snippet

selection: [0,0] => [2,14]

# Expecting a section but getting a field with continuations separated by idle lines

-- input
element: value
| continuation
| continuation

> comment
| continuation
-- input

-- javascript
parse(input).section('element');
-- javascript

-- php
Enolib\Parser::parse($input)->section('element');
-- php

-- python
enolib.parse(input).section('element')
-- python

-- ruby
Enolib.parse(input).section('element')
-- ruby

## ValidationError

-- text
A section with the key 'element' was expected.
-- text

-- snippet
   Line | Content
 >    1 | element: value
 *    2 | | continuation
 *    3 | | continuation
 *    4 | 
 *    5 | > comment
 *    6 | | continuation
-- snippet

selection: [0,0] => [5,14]

# Expecting a section but getting a flag

-- input
element
-- input

-- javascript
parse(input).section('element');
-- javascript

-- php
Enolib\Parser::parse($input)->section('element');
-- php

-- python
enolib.parse(input).section('element')
-- python

-- ruby
Enolib.parse(input).section('element')
-- ruby

## ValidationError

-- text
A section with the key 'element' was expected.
-- text

-- snippet
   Line | Content
 >    1 | element
-- snippet

selection: [0,0] => [0,7]
